version: 2

models:
  - name: 0501_null_key_data
    description: "incompleted ingestion data"
    columns:
      - name: NullKeyID

  - name: 0502_core_statistics
    description: "duplicate, orders, cancellation ingestion data"

  - name: 0504_mart_statistics
    description: "row counts per table per rep. month"
